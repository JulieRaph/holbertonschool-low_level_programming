#include "main.h"

/**
 * add - add two integers and returns the result
 * @b: entier en addition
 * @c: entier en addition
 * Return: always 0 (Success)
*/

int add(int b, int c)
{
	return (b + c);
}
