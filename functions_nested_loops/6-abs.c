#include "main.h"

/**
 * _abs - compute absolute value of an integer
 *
 * @x: valeur pour absolute
 *
 * Return: return value _abs
*/

int _abs(int x)
{
	if (x >= 0)
		return (x);
	else
		return (-x);
	return (0);
}
