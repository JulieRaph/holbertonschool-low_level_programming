#include "main.h"

/**
 * _isupper - fonction uppercase
 * @c: entier letter
 * Return: uppercase or lowarcase with a number
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1);
	else
		return (0);
}
